{{ 'section-multicolumn.css' | asset_url | stylesheet_tag }}
{{ 'component-slider.css' | asset_url | stylesheet_tag }}

{%- style -%}
  .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top | times: 0.75 | round: 0 }}px;
    padding-bottom: {{ section.settings.padding_bottom | times: 0.75 | round: 0 }}px;
  }

  @media screen and (min-width: 750px) {
    .section-{{ section.id }}-padding {
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
    }
  }

  /* Bubble/Pill Title Styles */
  .title--bubble {
    display: inline-block;
    background: {{ section.settings.bubble_background_color | default: '#007bff' }};
    color: {{ section.settings.bubble_text_color | default: '#ffffff' }};
    padding: {{ section.settings.bubble_padding_vertical | default: 8 }}px {{ section.settings.bubble_padding_horizontal | default: 16 }}px;
    border-radius: {{ section.settings.bubble_border_radius | default: 25 }}px;
    font-weight: {{ section.settings.bubble_font_weight | default: '500' }};
    font-size: {{ section.settings.bubble_font_size | default: 16 }}px;
    margin-bottom: 1rem;
    {% if section.settings.bubble_shadow %}
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
    {% endif %}
  }

  /* Center bubble alignment */
  .multicolumn__title--center {
    text-align: center;
  }

  /* Subtitle pill styles */
  .multicolumn__subtitle {
    display: inline-block;
    background: {{ section.settings.subtitle_background_color | default: '#007bff' }};
    color: {{ section.settings.subtitle_color | default: '#ffffff' }};
    padding: {{ section.settings.subtitle_padding_vertical | default: 8 }}px {{ section.settings.subtitle_padding_horizontal | default: 16 }}px;
    border-radius: {{ section.settings.subtitle_border_radius | default: 20 }}px;
    font-size: {{ section.settings.subtitle_size | default: 14 }}px;
    font-weight: {{ section.settings.subtitle_weight | default: '500' }};
    margin: 0 0 24px 0;
    line-height: 1.4;
    text-transform: {{ section.settings.subtitle_text_transform }};
    letter-spacing: {{ section.settings.subtitle_letter_spacing }}px;
  }

  .multicolumn__subtitle.has-shadow {
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
  }

  .multicolumn__subtitle.has-border {
    border: {{ section.settings.subtitle_border_width }}px solid {{ section.settings.subtitle_border_color | default: 'rgba(0, 0, 0, 0.1)' }};
  }

  .multicolumn__subtitle-wrapper {
    text-align: {{ section.settings.subtitle_alignment }};
    margin-bottom: 32px;
  }

  /* Description styles */
  .multicolumn__description {
    font-size: {{ section.settings.description_size | default: 16 }}px;
    color: {{ section.settings.description_color | default: '#666666' }};
    line-height: {{ section.settings.description_line_height | default: 1.6 }};
    text-align: {{ section.settings.description_alignment }};
    max-width: {{ section.settings.description_max_width }}px;
    margin: 0 auto 40px auto;
    font-weight: {{ section.settings.description_font_weight }};
  }

  /* Consistent image sizing styles */
  .multicolumn-card__image-wrapper--consistent {
    width: 100%;
    height: {{ section.settings.image_height | default: 200 }}px;
    overflow: hidden;
    display: flex;
    align-items: center;
    justify-content: center;
    background-color: {{ section.settings.image_background | default: '#f8f8f8' }};
    border-radius: {{ section.settings.image_border_radius | default: 8 }}px;
  }

  .multicolumn-card__image--consistent {
    width: 100%;
    height: 100%;
    object-fit: {{ section.settings.image_fit | default: 'contain' }};
    object-position: center;
  }

  /* Override default image wrapper when consistent sizing is enabled */
  .multicolumn--consistent-images .multicolumn-card__image-wrapper {
    width: 100% !important;
  }

  .multicolumn--consistent-images .media {
    padding-bottom: 0 !important;
    height: {{ section.settings.image_height | default: 200 }}px;
  }
{%- endstyle -%}

{%- liquid
  assign columns_mobile_int = section.settings.columns_mobile | plus: 0
  assign show_mobile_slider = false
  if section.settings.swipe_on_mobile and section.blocks.size > columns_mobile_int
    assign show_mobile_slider = true
  endif
  
  assign consistent_images_class = ''
  if section.settings.consistent_image_sizing
    assign consistent_images_class = ' multicolumn--consistent-images'
  endif

  assign title_class = ''
  if section.settings.title_style == 'bubble'
    assign title_class = ' title--bubble'
    if section.settings.bubble_shadow
      assign title_class = title_class | append: ' has-shadow'
    endif
    if section.settings.bubble_border
      assign title_class = title_class | append: ' has-border'
    endif
  endif

  assign subtitle_class = 'multicolumn__subtitle'
  if section.settings.subtitle_shadow
    assign subtitle_class = subtitle_class | append: ' has-shadow'
  endif
  if section.settings.subtitle_border
    assign subtitle_class = subtitle_class | append: ' has-border'
  endif

  assign title_alignment_class = ''
  if section.settings.bubble_center_align
    assign title_alignment_class = ' multicolumn__title--center'
  endif
-%}

<div class="multicolumn color-{{ section.settings.color_scheme }} gradient{% unless section.settings.background_style == 'none' and settings.text_boxes_border_thickness > 0 or settings.text_boxes_shadow_opacity > 0 %} background-{{ section.settings.background_style }}{% endunless %}{% if section.settings.title == blank %} no-heading{% endif %}{{ consistent_images_class }}">
  <div
    class="page-width section-{{ section.id }}-padding isolate{% if settings.animations_reveal_on_scroll %} scroll-trigger animate--slide-in{% endif %}"
    {% if settings.animations_reveal_on_scroll %}
      data-cascade
    {% endif %}
  >
    {%- if section.settings.subtitle != blank -%}
      <div class="multicolumn__subtitle-wrapper">
        <div class="{{ subtitle_class }}">{{ section.settings.subtitle }}</div>
      </div>
    {%- endif -%}
    
    {%- unless section.settings.title == blank -%}
      <div class="title-wrapper-with-link title-wrapper--self-padded-mobile title-wrapper--no-top-margin multicolumn__title{{ title_alignment_class }}">
        <h2 class="title inline-richtext {{ section.settings.heading_size }}{{ title_class }}">
          {{ section.settings.title }}
        </h2>
        {%- if section.settings.button_label != blank and show_mobile_slider -%}
          <a href="{{ section.settings.button_link }}" class="link underlined-link large-up-hide">
            {{- section.settings.button_label | escape -}}
          </a>
        {%- endif -%}
      </div>
    {%- endunless -%}

    {%- if section.settings.description != blank -%}
      <div class="multicolumn__description">{{ section.settings.description }}</div>
    {%- endif -%}
    
    <slider-component class="slider-mobile-gutter">
      <ul
        class="multicolumn-list contains-content-container grid grid--{{ section.settings.columns_mobile }}-col-tablet-down grid--{{ section.settings.columns_desktop }}-col-desktop{% if show_mobile_slider %} slider slider--tablet grid--peek{% endif %}"
        id="Slider-{{ section.id }}"
        role="list"
      >
        {%- liquid
          assign highest_ratio = 0
          for block in section.blocks
            if block.settings.image.aspect_ratio > highest_ratio
              assign highest_ratio = block.settings.image.aspect_ratio
            endif
          endfor
        -%}
        {%- for block in section.blocks -%}
          {%- assign empty_column = '' -%}
          {%- if block.settings.image == blank
            and block.settings.title == blank
            and block.settings.text == blank
            and block.settings.link_label == blank
          -%}
            {%- assign empty_column = ' multicolumn-list__item--empty' -%}
          {%- endif -%}

          <li
            id="Slide-{{ section.id }}-{{ forloop.index }}"
            class="multicolumn-list__item grid__item{% if section.settings.swipe_on_mobile %} slider__slide{% endif %}{% if section.settings.column_alignment == 'center' %} center{% endif %}{{ empty_column }}{% if settings.animations_reveal_on_scroll %} scroll-trigger animate--slide-in{% endif %}"
            {{ block.shopify_attributes }}
            {% if settings.animations_reveal_on_scroll %}
              data-cascade
              style="--animation-order: {{ forloop.index }};"
            {% endif %}
          >
            <div class="multicolumn-card content-container">
              {%- if block.settings.image != blank -%}
                {% if section.settings.consistent_image_sizing %}
                  <div class="multicolumn-card__image-wrapper--consistent multicolumn-card-spacing">
                    {%- liquid
                      assign number_of_columns = section.settings.columns_desktop
                      assign number_of_columns_mobile = section.settings.columns_mobile
                      assign grid_space_desktop = number_of_columns | minus: 1 | times: settings.spacing_grid_horizontal | plus: 100 | append: 'px'
                      assign grid_space_tablet = number_of_columns_mobile | minus: 1 | times: settings.spacing_grid_horizontal | plus: 100 | append: 'px'
                      assign grid_space_mobile = number_of_columns_mobile | minus: 1 | times: settings.spacing_grid_horizontal | divided_by: 2 | plus: 30 | append: 'px'
                      assign image_width = 1
                    -%}
                    {% capture sizes %}
                      (min-width: {{ settings.page_width }}px) calc(({{ settings.page_width }}px - {{ grid_space_desktop }}) * {{ image_width }} /  {{ number_of_columns }}),
                      (min-width: 990px) calc((100vw - {{ grid_space_desktop }}) * {{ image_width }} / {{ number_of_columns }}),
                      (min-width: 750px) calc((100vw - {{ grid_space_tablet }}) * {{ image_width }} / {{ number_of_columns_mobile }}),
                      calc((100vw - {{ grid_space_mobile }}) * {{ image_width }} / {{ number_of_columns_mobile }})
                    {% endcapture %}
                    {{
                      block.settings.image
                      | image_url: width: 800
                      | image_tag:
                        widths: '200, 300, 400, 500, 600, 800',
                        sizes: sizes,
                        class: 'multicolumn-card__image--consistent'
                    }}
                  </div>
                {% else %}
                  {% if section.settings.image_ratio == 'adapt' or section.settings.image_ratio == 'circle' %}
                    {% assign spaced_image = true %}
                  {% endif %}
                  <div class="multicolumn-card__image-wrapper multicolumn-card__image-wrapper--{{ section.settings.image_width }}-width{% if section.settings.image_width != 'full' or spaced_image %} multicolumn-card-spacing{% endif %}">
                    <div
                      class="media media--transparent media--{{ section.settings.image_ratio }}"
                      {% if section.settings.image_ratio == 'adapt' %}
                        style="padding-bottom: {{ 1 | divided_by: highest_ratio | times: 100 }}%;"
                      {% endif %}
                    >
                      {%- liquid
                        assign number_of_columns = section.settings.columns_desktop
                        assign number_of_columns_mobile = section.settings.columns_mobile
                        assign grid_space_desktop = number_of_columns | minus: 1 | times: settings.spacing_grid_horizontal | plus: 100 | append: 'px'
                        assign grid_space_tablet = number_of_columns_mobile | minus: 1 | times: settings.spacing_grid_horizontal | plus: 100 | append: 'px'
                        assign grid_space_mobile = number_of_columns_mobile | minus: 1 | times: settings.spacing_grid_horizontal | divided_by: 2 | plus: 30 | append: 'px'
                        if section.settings.image_width == 'half'
                          assign image_width = 0.5
                        elsif section.settings.image_width == 'third'
                          assign image_width = 0.33
                        else
                          assign image_width = 1
                        endif
                      -%}
                      {% capture sizes %}
                        (min-width: {{ settings.page_width }}px) calc(({{ settings.page_width }}px - {{ grid_space_desktop }}) * {{ image_width }} /  {{ number_of_columns }}),
                        (min-width: 990px) calc((100vw - {{ grid_space_desktop }}) * {{ image_width }} / {{ number_of_columns }}),
                        (min-width: 750px) calc((100vw - {{ grid_space_tablet }}) * {{ image_width }} / {{ number_of_columns_mobile }}),
                        calc((100vw - {{ grid_space_mobile }}) * {{ image_width }} / {{ number_of_columns_mobile }})
                      {% endcapture %}
                      {{
                        block.settings.image
                        | image_url: width: 3200
                        | image_tag:
                          widths: '50, 75, 100, 150, 200, 300, 400, 500, 750, 1000, 1250, 1500, 1750, 2000, 2250, 2500, 2750, 3000, 3200',
                          sizes: sizes,
                          class: 'multicolumn-card__image'
                      }}
                    </div>
                  </div>
                {% endif %}
              {%- endif -%}
              <div class="multicolumn-card__info">
                {%- if block.settings.title != blank -%}
                  <h3 class="inline-richtext">{{ block.settings.title }}</h3>
                {%- endif -%}
                {%- if block.settings.text != blank -%}
                  <div class="rte">{{ block.settings.text }}</div>
                {%- endif -%}
                {%- if block.settings.link_label != blank -%}
                  <a
                    class="link animate-arrow"
                    {% if block.settings.link == blank %}
                      role="link" aria-disabled="true"
                    {% else %}
                      href="{{ block.settings.link }}"
                    {% endif %}
                  >
                    {{- block.settings.link_label | escape -}}
                    <span class="svg-wrapper"
                      ><span class="icon-wrap">&nbsp;{{ 'icon-arrow.svg' | inline_asset_content }}</span></span
                    >
                  </a>
                {%- endif -%}
              </div>
            </div>
          </li>
        {%- endfor -%}
      </ul>

      {%- if show_mobile_slider -%}
        <div class="slider-buttons large-up-hide">
          <button
            type="button"
            class="slider-button slider-button--prev"
            name="previous"
            aria-label="{{ 'general.slider.previous_slide' | t }}"
          >
            <span class="svg-wrapper">{{ 'icon-caret.svg' | inline_asset_content }}</span>
          </button>
          <div class="slider-counter caption">
            <span class="slider-counter--current">1</span>
            <span aria-hidden="true"> / </span>
            <span class="visually-hidden">{{ 'general.slider.of' | t }}</span>
            <span class="slider-counter--total">{{ section.blocks.size }}</span>
          </div>
          <button
            type="button"
            class="slider-button slider-button--next"
            name="next"
            aria-label="{{ 'general.slider.next_slide' | t }}"
          >
            <span class="svg-wrapper">{{ 'icon-caret.svg' | inline_asset_content }}</span>
          </button>
        </div>
      {%- endif -%}
    </slider-component>
    <div class="center{% if show_mobile_slider %} small-hide medium-hide{% endif %}">
      {%- if section.settings.button_label != blank -%}
        <a
          class="button button--primary"
          {% if section.settings.button_link == blank %}
            role="link" aria-disabled="true"
          {% else %}
            href="{{ section.settings.button_link }}"
          {% endif %}
        >
          {{ section.settings.button_label | escape }}
        </a>
      {%- endif -%}
    </div>
  </div>
</div>

{% schema %}
{
  "name": "Enhanced Multicolumn",
  "class": "section",
  "tag": "section",
  "disabled_on": {
    "groups": ["header", "footer"]
  },
  "settings": [
    {
      "type": "header",
      "content": "Subtitle Settings"
    },
    {
      "type": "textarea",
      "id": "subtitle",
      "label": "Subtitle",
      "default": "Customer Reviews",
      "info": "Small pill-style subtitle that appears above the main title"
    },
    {
      "type": "color",
      "id": "subtitle_background_color",
      "label": "Subtitle Background Color",
      "default": "#007bff"
    },
    {
      "type": "color",
      "id": "subtitle_color",
      "label": "Subtitle Text Color",
      "default": "#ffffff"
    },
    {
      "type": "range",
      "id": "subtitle_size",
      "min": 10,
      "max": 18,
      "step": 1,
      "unit": "px",
      "label": "Subtitle Font Size",
      "default": 14
    },
    {
      "type": "range",
      "id": "subtitle_padding_vertical",
      "min": 4,
      "max": 16,
      "step": 1,
      "unit": "px",
      "label": "Subtitle Vertical Padding",
      "default": 8
    },
    {
      "type": "range",
      "id": "subtitle_padding_horizontal",
      "min": 8,
      "max": 32,
      "step": 2,
      "unit": "px",
      "label": "Subtitle Horizontal Padding",
      "default": 16
    },
    {
      "type": "range",
      "id": "subtitle_border_radius",
      "min": 8,
      "max": 40,
      "step": 2,
      "unit": "px",
      "label": "Subtitle Border Radius",
      "default": 20
    },
    {
      "type": "select",
      "id": "subtitle_weight",
      "options": [
        {
          "value": "400",
          "label": "Normal"
        },
        {
          "value": "500",
          "label": "Medium"
        },
        {
          "value": "600",
          "label": "Semi Bold"
        },
        {
          "value": "700",
          "label": "Bold"
        }
      ],
      "default": "500",
      "label": "Subtitle Font Weight"
    },
    {
      "type": "select",
      "id": "subtitle_text_transform",
      "options": [
        {
          "value": "none",
          "label": "None"
        },
        {
          "value": "uppercase",
          "label": "Uppercase"
        },
        {
          "value": "lowercase",
          "label": "Lowercase"
        },
        {
          "value": "capitalize",
          "label": "Capitalize"
        }
      ],
      "default": "none",
      "label": "Subtitle Text Transform"
    },
    {
      "type": "range",
      "id": "subtitle_letter_spacing",
      "min": -1,
      "max": 3,
      "step": 0.1,
      "unit": "px",
      "label": "Subtitle Letter Spacing",
      "default": 0
    },
    {
      "type": "select",
      "id": "subtitle_alignment",
      "options": [
        {
          "value": "left",
          "label": "Left"
        },
        {
          "value": "center",
          "label": "Center"
        },
        {
          "value": "right",
          "label": "Right"
        }
      ],
      "default": "center",
      "label": "Subtitle Alignment"
    },
    {
      "type": "checkbox",
      "id": "subtitle_border",
      "default": false,
      "label": "Add border to subtitle"
    },
    {
      "type": "range",
      "id": "subtitle_border_width",
      "min": 1,
      "max": 4,
      "step": 1,
      "unit": "px",
      "label": "Subtitle Border Width",
      "default": 1,
      "info": "Only applies when subtitle border is enabled"
    },
    {
      "type": "color",
      "id": "subtitle_border_color",
      "label": "Subtitle Border Color",
      "default": "rgba(0, 0, 0, 0.1)",
      "info": "Only applies when subtitle border is enabled"
    },
    {
      "type": "checkbox",
      "id": "subtitle_shadow",
      "default": true,
      "label": "Add shadow to subtitle"
    },
    {
      "type": "header",
      "content": "Title Settings"
    },
    {
      "type": "inline_richtext",
      "id": "title",
      "default": "What Our Customers Say",
      "label": "Main Title"
    },
    {
      "type": "select",
      "id": "heading_size",
      "options": [
        {
          "value": "h2",
          "label": "Small"
        },
        {
          "value": "h1",
          "label": "Medium"
        },
        {
          "value": "h0",
          "label": "Large"
        },
        {
          "value": "hxl",
          "label": "Extra Large"
        },
        {
          "value": "hxxl",
          "label": "XX Large"
        }
      ],
      "default": "h1",
      "label": "Title Size"
    },
    {
      "type": "select",
      "id": "title_style",
      "options": [
        {
          "value": "default",
          "label": "Default"
        },
        {
          "value": "bubble",
          "label": "Bubble/Pill Style"
        }
      ],
      "default": "default",
      "label": "Title Style"
    },
    {
      "type": "color",
      "id": "bubble_background_color",
      "label": "Bubble Background Color",
      "default": "#007bff",
      "info": "Only applies when bubble style is selected"
    },
    {
      "type": "color",
      "id": "bubble_text_color",
      "label": "Bubble Text Color",
      "default": "#ffffff",
      "info": "Only applies when bubble style is selected"
    },
    {
      "type": "range",
      "id": "bubble_font_size",
      "min": 12,
      "max": 24,
      "step": 1,
      "unit": "px",
      "label": "Bubble Font Size",
      "default": 16,
      "info": "Only applies when bubble style is selected"
    },
    {
      "type": "range",
      "id": "bubble_padding_vertical",
      "min": 4,
      "max": 20,
      "step": 1,
      "unit": "px",
      "label": "Bubble Vertical Padding",
      "default": 8,
      "info": "Only applies when bubble style is selected"
    },
    {
      "type": "range",
      "id": "bubble_padding_horizontal",
      "min": 8,
      "max": 40,
      "step": 2,
      "unit": "px",
      "label": "Bubble Horizontal Padding",
      "default": 16,
      "info": "Only applies when bubble style is selected"
    },
    {
      "type": "range",
      "id": "bubble_border_radius",
      "min": 10,
      "max": 50,
      "step": 5,
      "unit": "px",
      "label": "Bubble Border Radius",
      "default": 25,
      "info": "Only applies when bubble style is selected"
    },
    {
      "type": "select",
      "id": "bubble_font_weight",
      "options": [
        {
          "value": "400",
          "label": "Normal"
        },
        {
          "value": "500",
          "label": "Medium"
        },
        {
          "value": "600",
          "label": "Semi Bold"
        },
        {
          "value": "700",
          "label": "Bold"
        }
      ],
      "default": "500",
      "label": "Bubble Font Weight",
      "info": "Only applies when bubble style is selected"
    },
    {
      "type": "checkbox",
      "id": "bubble_center_align",
      "default": false,
      "label": "Center align bubble",
      "info": "Only applies when bubble style is selected"
    },
    {
      "type": "checkbox",
      "id": "bubble_shadow",
      "default": true,
      "label": "Add shadow to bubble",
      "info": "Only applies when bubble style is selected"
    },
    {
      "type": "header",
      "content": "Description Settings"
    },
    {
      "type": "textarea",
      "id": "description",
      "label": "Description",
      "default": "Don't just take our word for it. Here's what our satisfied customers have to say about our products.",
      "info": "Description text that appears below the title"
    },
    {
      "type": "color",
      "id": "description_color",
      "label": "Description Text Color",
      "default": "#666666"
    },
    {
      "type": "range",
      "id": "description_size",
      "min": 12,
      "max": 22,
      "step": 1,
      "unit": "px",
      "label": "Description Font Size",
      "default": 16
    },
    {
      "type": "range",
      "id": "description_line_height",
      "min": 1.2,
      "max": 2.0,
      "step": 0.1,
      "label": "Description Line Height",
      "default": 1.6
    },
    {
      "type": "select",
      "id": "description_alignment",
      "options": [
        {
          "value": "left",
          "label": "Left"
        },
        {
          "value": "center",
          "label": "Center"
        },
        {
          "value": "right",
          "label": "Right"
        }
      ],
      "default": "center",
      "label": "Description Alignment"
    },
    {
      "type": "range",
      "id": "description_max_width",
      "min": 400,
      "max": 1000,
      "step": 50,
      "unit": "px",
      "label": "Description Max Width",
      "default": 600
    },
    {
      "type": "select",
      "id": "description_font_weight",
      "options": [
        {
          "value": "300",
          "label": "Light"
        },
        {
          "value": "400",
          "label": "Normal"
        },
        {
          "value": "500",
          "label": "Medium"
        },
        {
          "value": "600",
          "label": "Semi Bold"
        }
      ],
      "default": "400",
      "label": "Description Font Weight"
    },
    {
      "type": "header",
      "content": "Image Settings"
    },
    {
      "type": "checkbox",
      "id": "consistent_image_sizing",
      "default": true,
      "label": "Enable consistent image sizing",
      "info": "Forces all images to the same dimensions for a uniform look"
    },
    {
      "type": "range",
      "id": "image_height",
      "min": 100,
      "max": 400,
      "step": 20,
      "unit": "px",
      "label": "Image height (consistent sizing)",
      "default": 200,
      "info": "Only applies when consistent image sizing is enabled"
    },
    {
      "type": "select",
      "id": "image_fit",
      "options": [
        {
          "value": "contain",
          "label": "Contain (fit entire image)"
        },
        {
          "value": "cover",
          "label": "Cover (fill container)"
        },
        {
          "value": "fill",
          "label": "Fill (stretch to fit)"
        }
      ],
      "default": "contain",
      "label": "Image fit (consistent sizing)",
      "info": "How images should fit within the consistent dimensions"
    },
    {
      "type": "color",
      "id": "image_background",
      "label": "Image background color",
      "default": "#f8f8f8",
      "info": "Background color visible when using 'contain' fit"
    },
    {
      "type": "range",
      "id": "image_border_radius",
      "min": 0,
      "max": 20,
      "step": 2,
      "unit": "px",
      "label": "Image border radius",
      "default": 8
    },
    {
      "type": "select",
      "id": "image_width",
      "options": [
        {
          "value": "third",
          "label": "One third"
        },
        {
          "value": "half",
          "label": "One half"
        },
        {
          "value": "full",
          "label": "Full width"
        }
      ],
      "default": "full",
      "label": "Image width",
      "info": "Only applies when consistent image sizing is disabled"
    },
    {
      "type": "select",
      "id": "image_ratio",
      "options": [
        {
          "value": "adapt",
          "label": "Adapt to image"
        },
        {
          "value": "portrait",
          "label": "Portrait"
        },
        {
          "value": "square",
          "label": "Square"
        },
        {
          "value": "circle",
          "label": "Circle"
        }
      ],
      "default": "adapt",
      "label": "Image ratio",
      "info": "Only applies when consistent image sizing is disabled"
    },
    {
      "type": "header",
      "content": "Button Settings"
    },    
    {
      "type": "text",
      "id": "button_label",
      "default": "View All Reviews",
      "label": "Button label", 
      "info": "Leave blank to hide the button"
    },
    {
      "type": "url",
      "id": "button_link",
      "label": "Button link"
    },    
    {
      "type": "header",
      "content": "Layout Settings"
    },     
    {
      "type": "range",
      "id": "columns_desktop",
      "min": 1,
      "max": 6,
      "step": 1,
      "default": 3,
      "label": "Number of columns on desktop"
    },
    {
      "type": "select",
      "id": "column_alignment",
      "options": [
        {
          "value": "left",
          "label": "Left"
        },
        {
          "value": "center",
          "label": "Center"
        }
      ],
      "default": "left",
      "label": "Column alignment"
    },
    {
      "type": "select",
      "id": "background_style",
      "options": [
        {
          "value": "none",
          "label": "None"
        },
        {
          "value": "primary",
          "label": "Primary"
        }
      ],
      "default": "primary",
      "label": "Background style"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Color scheme",
      "default": "scheme-1"
    },    
    {
      "type": "header",
      "content": "Mobile Settings"
    },
    {
      "type": "select",
      "id": "columns_mobile",
      "options": [
        {
          "value": "1",
          "label": "1 column"
        },
        {
          "value": "2",
          "label": "2 columns"
        }
      ],
      "default": "1",
      "label": "Number of columns on mobile"
    },
    {
      "type": "checkbox",
      "id": "swipe_on_mobile",
      "default": false,
      "label": "Enable swiping on mobile"
    },
    {
      "type": "header",
      "content": "Section padding"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Top padding",
      "default": 36
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Bottom padding",
      "default": 36
    }
  ],
  "blocks": [
    {
      "type": "column",
      "name": "Column",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image"
        },
        {
          "type": "inline_richtext",
          "id": "title",
          "default": "Column",
          "label": "Heading"
        },
        {
          "type": "richtext",
          "id": "text",
          "default": "<p>Pair text with an image to focus on your chosen product, collection, or blog post. Add details on availability, style, or even provide a review.</p>",
          "label": "Description"
        },
        {
          "type": "text",
          "id": "link_label",
          "label": "Link label",
          "info": "Leave blank to hide the link"
        },
        {
          "type": "url",
          "id": "link",
          "label": "Link"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Enhanced Multicolumn",
      "settings": {
        "subtitle": "Customer Reviews",
        "title": "What Our Customers Say",
        "description": "Don't just take our word for it. Here's what our satisfied customers have to say about our products."
      },
      "blocks": [
        {
          "type": "column",
          "settings": {
            "title": "Amazing Quality",
            "text": "<p>\"The product exceeded my expectations in every way. Quality materials and excellent craftsmanship.\"</p>"
          }
        },
        {
          "type": "column",
          "settings": {
            "title": "Fast Shipping",
            "text": "<p>\"Order arrived quickly and was packaged perfectly. Great customer service throughout the process.\"</p>"
          }
        },
        {
          "type": "column",
          "settings": {
            "title": "Great Value",
            "text": "<p>\"Outstanding value for money. This purchase was definitely worth every penny spent.\"</p>"
          }
        }
      ]
    }
  ]
}
{% endschema %}